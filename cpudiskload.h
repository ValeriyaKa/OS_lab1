#ifndef MULTIPLAYLOAD_H
#define MULTIPLAYLOAD_H

#include <cstddef> 
#include <string>

void RunCombinedLoad(size_t cpuThreads, size_t cpuIterations, size_t diskIterations, unsigned long long number);

#endif 